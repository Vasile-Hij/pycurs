Terminal log - week2/curs4
---
[Introduction to git](https://speakerdeck.com/schacon/introduction-to-git)

[Git Handbook](https://guides.github.com/introduction/git-handbook/)

[Git on youtube](https://www.youtube.com/watch?v=ZDR433b0HJY)

```
$ mkdir Curs4
$ cd Curs4
$ git init
$ git status
$ touch file.txt
$ git status
$ git add file.txt
$ git status
$ vi file.txt
$ git status
$ git log
$ git status
$ touch file2.txt
$ git status
$ git commit -m 'Al doilea commit'
$ git log
$ git status
$ git add file2.txt
$ git status
$ git commit -m 'Al treilea commit'
$ git status
$ git log
$ git branch
$ git checkout -b feature
$ git branch
$ git log
$ touch branch.txt
$ git add .
$ git status
$ git commit
$ git status
$ git log
$ git checkout master
$ git merge feature
$ git checkout feature
$ git status
$ git log
$ git checkout
$ git checkout master
$ git status
$ git log
$ git branch
$ git checkout -b feature
$ git log
$ touch
$ git checkout feature
$ vi branch.txt
$ git commit -m 'Conflicts'
$ git log
$ git add branch.txt
$ git log
$ git bracnh
$ git branch
$ git merge feature
$ git branch
$ git log
$ git status
$ git checkout master
$ git log
$ git status
$ git add .
$ git commit 'Another conflict'
$ git commit -m 'Another conflict'
$ git log
$ git status
$ git commit
$ git checkout feature
$ git add .
$ git status
$ git checkout master
$ git status
$ git log
$ git push origin mster
$ git branch
$ git push origin master
$ git log
$ git commit
$ git fetch
$ git branch
$ git -rm
$ cd
$ ssh-keygen -t rsa -b 4096
$ cd Wantsome/Curs4
$ mkdir Gitlab
$ cd Gitlab
$ git clone git@gitlab.com:Vasile-Hij/wantsome-python-demo-vasile-hij.git
$ cd
$ cd git
$ git clone git@gitlab.com:Vasile-Hij/wantsome-python-demo-vasile-hij.git
$ ls 
$ cd ls
$ cd $!
$ git wantsome-python-demo-vasile-hij
$ ls
$ cd Wantsome
$ git clone git@gitlab.com:Vasile-Hij/pycurs.git
$ cd pycurs
$ vi readme.md
$ cd Wantosme
$ cd Wantsome
$ mkdir week1
$ mkdir week2
$ mkdir week3
$ exit
```