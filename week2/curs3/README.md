Terminal log - Week2/Curs3
---
```
$ mkdir Curs3
$ cd Curs3
$ ls -latr
$ tree.
$ git init
$ tree
$ ls -al
$ cat .git
$ cat ./.git
$ tocuh cuprins
$ touch cuprins
$ touch capitolul-1
$ touch capitolul-2
$ ls
$ git status
$ vim capitolul-1
$ git status
$ git add capitolul-1
$ git status
$ git commit
$ cd Wantsome/Curs3
$ git status
$ git add cuprins
$ git status
$ vim cuprins
$ git status
$ cat cuprins
$ tree ./git
$ tree cat-file
$ git cat-file -p
$ vim cuprins
$ git status
$ git tree
$ tree
$ git tree
$ git reset capitolul-1
$ gi status
$ git status
$ git add cuprins
$ git status
$ cat cuprins
$ cat capitolul-1
$ git commit -m 'Adauga cuprinsul'
$ commit
$ git log
$ git add capitolul-1
$ git commit -m 'Acutalizeaza capitolul I'
$ git add capitolul-2
$ git add commit -m 'Adaugat'
$ git log
$ ls -l
$ git branch capitolul-3
$ git branch
$ git branch capitolul-4
$ git log
$ git checkout capitolul-3
$ touch capitolul-3
$ git status
$ git add capitolul-3
$ git status
$ git commit -m '--Capitolul 3--'
$ git log
$ touch capitolul-4
$ git add capitolul-4
$ git log
$ git commit -m '--Capitolul 4--'
$ git log
$ git checkout capitolul-3
$ git checkout capitolul-4
$ git log
$ git branch
$ git merge capitolul-4
$ git log -graph
$ git -graph
$ git log --graph
$ git checkout master
$ git log --master
$ git log master
$ git checkout -b vasi
$ git log --graph
$ git branch
$ git branch test
$ git branch
$ git checkout test
$ git brach
$ git brang
$ git branch
$ git log
$ git checkout masster
$ git checkout master
$ git log --graph
$ master | test
$ exit
```